import postAccessToken from './token'
import { postCreateBucket, getBucketList, deleteBucket } from './buckets'

export {
    postAccessToken,
    postCreateBucket,
    getBucketList,
    deleteBucket
}
